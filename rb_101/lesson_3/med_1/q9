def foo(param = "no")
  "yes"
end

def bar(param = "no")
  param == "no" ? "yes" : "no"
end

bar(foo)
--------------------------------
manual evaluation

foo call => returns "yes"
bar called with argument "yes" => param = "yes", return value is "no"
